# Environment configuration for local development
# This file is used when running locally without Docker

# Database Configuration (localhost)
DB_HOST=localhost
DB_PORT=5432
DB_USER=statement_user
DB_PASS=statement_password
DB_NAME=statement_sense

# Project Configuration
PROJECT_NAME=StatementSense

# Ollama Configuration (localhost)
OLLAMA_URL=http://localhost:11434

# Logging
LOG_LEVEL=INFO

# Development Settings
ENVIRONMENT=development
DEBUG=true

# File Upload Settings
MAX_FILE_SIZE=50MB
UPLOAD_DIR=./uploads
ALLOWED_EXTENSIONS=pdf

# Security (generate these for production)
SECRET_KEY=your-secret-key-here-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
